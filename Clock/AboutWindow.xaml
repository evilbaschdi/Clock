<!-- ReSharper disable once Xaml.StaticResourceNotResolved -->
<ui:UiWindow x:Class="Clock.AboutWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             Style="{StaticResource UiWindow}"
             ExtendsContentIntoTitleBar="True"
             WindowBackdropType="Mica"
             WindowCornerPreference="Round"
             WindowStartupLocation="CenterScreen"
             xmlns:baschdi="clr-namespace:EvilBaschdi.WpfControls;assembly=EvilBaschdi.WpfControls"
             mc:Ignorable="d"
             ResizeMode="NoResize"
             Title="AboutWindow" Height="460" Width="530">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Icon="clock.ico" ShowMaximize="False" />

        <Grid Grid.Row="1">

            <StackPanel>
                <baschdi:AboutControl />
            </StackPanel>
        </Grid>
    </Grid>
</ui:UiWindow>